# NextAuth Configuration
NEXTAUTH_URL=http://127.0.0.1:3000
# generate a long random string with `openssl rand -base64 32` or similar
NEXTAUTH_SECRET=your-nextauth-secret-key-here

# Go to https://developer.spotify.com/dashboard to create an app and get these values
# Spotify API Configuration
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret

# Database Configuration
# For Docker development environment:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/firebird_dev
# For production (Supabase):
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Redis Configuration (optional - for caching)
# For Docker development environment:
REDIS_URL=redis://localhost:6379
# For production (Upstash):
# REDIS_URL=redis://[YOUR-UPSTASH-URL]:[PORT]?password=[YOUR-PASSWORD] 